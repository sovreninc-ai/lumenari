**To:** michael@cursor.com (placeholder — Michael Truell, founder)
**From:** chris@lumenari.io
**Subject:** Saw the agent mode shipping — proposal

Michael,

Agent mode + the codebase indexing has made Cursor the only AI editor I keep open all day. The context window strategy (chunking + retrieval + recent-files) is genuinely state of the art — I notice when I'm forced into another tool.

Sending a small idea. The bar's high, so I'll keep it tight.

Cursor users who open a fresh project don't get the benefit of the index yet — there's nothing to retrieve. That's the moment a curated skill kit would do the most work. "New Next.js + Supabase project" → Cursor suggests loading the `ts-next-production` + `supabase-schema-rls` kits as `.cursorrules`. User accepts. The first hour of every project is now senior-level instead of generic.

Lumenari is the API for that. 100 curated kits, native `.cursorrules` export format, REST `/recommend` endpoint. We handle the catalog and the matching; you keep the surface.

Happy to walk through it in 15 min if there's interest. Otherwise no worries — figured it was worth the note.

— Chris
Founder, Lumenari
lumenari.io

*Why this email: acknowledges Cursor's brand bar upfront, names a real product detail (index + retrieval), and proposes a specific surface (new-project moment) rather than a vague "partnership."*
