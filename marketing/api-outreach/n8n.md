**To:** jan@n8n.io (placeholder — Jan Oberhauser, founder)
**From:** chris@lumenari.io
**Subject:** Lumenari kits as n8n nodes?

Jan,

n8n is the only workflow tool I'd actually self-host — the fair-code license, the AI nodes you shipped in 2024, and the fact that the Code node is just JS instead of some DSL means power users never hit a ceiling. Zapier feels like a toy after you've built in n8n.

Pitch is fast. n8n users wiring up an AI node (OpenAI, Anthropic, etc.) write the system prompt themselves. The output is whatever their prompting skill produces. Workflows that should be "senior teammate doing the work" end up as "junior intern doing the work" because the system prompt is generic.

Lumenari is a REST API of 100 curated skill kits. The play: a "Lumenari" node (or a built-in option on the AI nodes) that takes a use case → returns a `SKILL.md` to drop into the system prompt field. Self-host friendly, single bearer token, no other setup.

Free tier 100 calls/mo. Pro $99 CAD/mo for 10K. Happy to ship a node spec or community-build it ourselves.

Want a 15-min call?

— Chris
Founder, Lumenari
lumenari.io

*Why this email: opens with the fair-code license + Code node — both are real n8n differentiators that the founder cares about. Pitches both an official integration and a community-built node (gives n8n team an easy out if they're slammed).*
