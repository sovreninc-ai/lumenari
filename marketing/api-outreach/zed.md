**To:** nathan@zed.dev (placeholder — Nathan Sobo, co-founder)
**From:** chris@lumenari.io
**Subject:** Zed + Lumenari?

Nathan,

Zed is the only editor where I can feel the perf difference within five minutes of opening it — the Rust foundation pays off in the input latency more than any benchmark captures. The native Anthropic integration via the assistant panel is the cleanest implementation I've used; no extension layer, no jank.

The pitch is short. Your assistant panel is great when the user knows what to ask. The "blank prompt" problem is the same one every AI editor has — premium positioning doesn't fix it because it's a user-knowledge gap, not a tool gap.

Lumenari is a REST API of 100 curated skill kits — `SKILL.md` files that turn Claude into a senior teammate for a specific pattern. One `/api/v1/recommend` call, user describes intent, kit loads into the assistant panel as context. Native markdown export, plays well with how Zed already handles context files.

Free tier 100 calls/mo. Could ship as a Zed extension or a first-party panel integration — your call.

Worth 15 min?

— Chris
Founder, Lumenari
lumenari.io

*Why this email: Zed's identity is perf + craft — leads with the input latency observation, which only a real user notices. Frames the integration as either an extension or first-party (gives them control of the surface).*
